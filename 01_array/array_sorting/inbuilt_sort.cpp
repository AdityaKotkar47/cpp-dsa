#include <bits/stdc++.h>

using namespace std;

int main() {

    // to be preferred when sorting is a secondary problem
    int n = 8;
    int arr[n] = {1, 4, 1, 3, 2, 4, 3, 7};
    sort(arr, arr+n);

    return 0;
}